/**
 * Stable download URL for the macOS desktop build.
 *
 * GitHub Release asset URLs follow a permanent format
 * (`/releases/download/<tag>/<asset-name>`), so this link stays valid as long
 * as the release is not deleted. Bump the tag here when a new desktop version
 * is published.
 */
export const DESKTOP_APP_DOWNLOAD_URL =
  "https://github.com/honlnk/gpt-image-studio/releases/download/desktop-v0.1.0/GPT.Image.Studio_0.1.0_aarch64.dmg";

/** Release page (for users who want to read version notes first). */
export const DESKTOP_APP_RELEASE_URL =
  "https://github.com/honlnk/gpt-image-studio/releases/tag/desktop-v0.1.0";
